name: Destroy DigitalOcean JIT Runner
on:
  workflow_call:
    inputs:
      droplet_id:
        required: true
        type: string

jobs:
  destroy-runner:
    runs-on: ubuntu-latest
    steps:
      - name: Destroy droplet
        run: |
          DROPLET_ID="${{ inputs.droplet_id }}"
          if [ -n "$DROPLET_ID" ]; then
            echo "🧹 Destroying droplet (ID: $DROPLET_ID)..."
            curl -s -X DELETE \
              -H "Authorization: Bearer ${{ secrets.DO_TOKEN }}" \
              "https://api.digitalocean.com/v2/droplets/$DROPLET_ID"
              echo "✅ Droplet destroyed successfully."
          else
            echo "⚠️ No droplet ID found — skipping destroy."
          fi